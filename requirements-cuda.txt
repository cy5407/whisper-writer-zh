# Conditional CUDA runtime dependencies for Windows.
# Install only if running the local Whisper model with compute_type != 'int8'
# (i.e. cuda/float16 or cuda/float32). Skip on CPU-only setups.
#
#   pip install -r requirements-cuda.txt
#
nvidia-cuda-runtime-cu12
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.21.1.3
